# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'electrolux flickr' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for electrolux flickr
  pod 'Alamofire', '~> 5.5.0'
  pod 'IQKeyboardManagerSwift', '~> 6.5.9'
  pod 'Kingfisher', '~> 7.2.0'
  pod 'SwiftLint'

  target 'electrolux flickrTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'electrolux flickrUITests' do
    # Pods for testing
  end

end
